const layer=document.querySelector('.particles');if(layer&&!matchMedia('(prefers-reduced-motion: reduce)').matches){for(let i=0;i<28;i+=1){const particle=document.createElement('i');particle.className='particle';particle.style.setProperty('--x',`${Math.random()*100}%`);particle.style.setProperty('--s',`${1+Math.random()*3}px`);particle.style.setProperty('--d',`${7+Math.random()*10}s`);particle.style.setProperty('--delay',`${-Math.random()*16}s`);layer.appendChild(particle)}}
